diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index c2a4be0dd..4ec5f3cbe 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -60,6 +60,7 @@ add_library(llama
             llama-kv-cache.cpp
             llama-kv-cache-iswa.cpp
             llama-kv-cache-dsa.cpp
+            llama-kv-cache-dsa-iswa.cpp
             llama-kv-cache-msa.cpp
             llama-kv-cache-dsv4.cpp
             llama-kv-stream-config.cpp
diff --git a/src/llama-context.cpp b/src/llama-context.cpp
index 64e4b45fd..c98dbe5a8 100644
--- a/src/llama-context.cpp
+++ b/src/llama-context.cpp
@@ -20,6 +20,7 @@
 #include "llama-memory.h"
 #include "llama-mmap.h"
 #include "llama-model.h"
+#include "llama-sampler.h"
 #include "llama.h"
 
 #include <algorithm>
@@ -1032,7 +1033,12 @@ bool llama_context::kv_stream_switch_phase(bool decode, uint32_t active_tokens)
 
     // Releasing the scheduler returns the only borrowed compute lease.
     sched.reset();
-    gf_res_prev->reset();
+    for (auto & res : gf_res_prev) {
+        if (res) {
+            res->reset();
+        }
+    }
+    gf_res_prev_active = nullptr;
 
     const size_t   old_kv_bytes       = arena.current_kv_bytes;
     const size_t   old_compute_offset = arena.current_compute_offset;
@@ -2215,7 +2221,7 @@ llm_graph_result * llama_context::process_ubatch(const llama_ubatch &     ubatch
         }
     }
 
-    auto * res = gf_res_prev.get();
+    auto * res = get_gf_res_prev();
     auto * gf  = res->get_gf();
 
     // the new graph parameters
@@ -2536,13 +2542,14 @@ static void copy_tensor_async_floats(const std::map<llama_seq_id, ggml_tensor *>
         return;
     }
 
-    for (size_t i = 0; i < tensors.size(); ++i) {
-        auto * tensor = tensors[i];
-        if (tensor == nullptr) {
+    for (const auto & [seq_id, tensor] : tensor_map) {
+        auto it = seq_to_row.find(seq_id);
+        if (it == seq_to_row.end()) {
             continue;
         }
 
-        const uint32_t row = row_offset + i;
+        const uint32_t row = it->second;
+        GGML_ASSERT(row < counts.size());
         const size_t n_elements = ggml_nelements(tensor);
         GGML_ASSERT(ggml_is_contiguous(tensor) && "sampling tensor must be contiguous for async copy");
         GGML_ASSERT(n_elements <= stride);
@@ -3296,6 +3303,7 @@ uint32_t llama_context::graph_max_nodes(uint32_t n_tokens) const {
         return std::max<uint32_t>(1024u, 12u * model.n_tensors());
     }
 
+    uint32_t res;
     if (model.arch == LLM_ARCH_QWEN3NEXT ||
         model.arch == LLM_ARCH_KIMI_LINEAR ||
         model.arch == LLM_ARCH_BAILINGMOE3 ||
@@ -3309,20 +3317,12 @@ uint32_t llama_context::graph_max_nodes(uint32_t n_tokens) const {
         model.arch == LLM_ARCH_MINIMAX_M3 ||
         model.arch == LLM_ARCH_HY_V4) {
         res = std::max<uint32_t>(n_tokens * 40, 32u * model.n_tensors());
-    } else if (model.arch == LLM_ARCH_DFLASH && model.hparams.dflash_selector_rank > 0) {
-        // DFlash2's convolutions and selector are shape work rather than matmuls,
-        // so they cost ~8.6 nodes per tensor against ~5.9 for a plain DFlash draft
-        res = std::max<uint32_t>(1024u, 12u*model.n_tensors());
     } else {
         res = std::max<uint32_t>(1024u, 8u*model.n_tensors());
         for (const auto & lora : model.loras) {
             res += lora->get_n_nodes();
         }
     }
-    uint32_t res = std::max<uint32_t>(1024u, 8u * model.n_tensors());
-    for (const auto & lora : model.loras) {
-        res += lora->get_n_nodes();
-    }
     return res;
 }
 
@@ -3330,6 +3330,14 @@ llm_graph_result * llama_context::get_gf_res_reserve() const {
     return static_cast<llm_graph_result *>(gf_res_reserve.get());
 }
 
+llm_graph_result * llama_context::get_gf_res_prev() {
+    auto & res = gf_res_prev[n_outputs > 0];
+    if (!res) {
+        res.reset(new llm_graph_result(gf_res_reserve->get_max_nodes()));
+    }
+    return res.get();
+}
+
 ggml_cgraph * llama_context::graph_reserve(uint32_t                       n_tokens,
                                            uint32_t                       n_seqs,
                                            uint32_t                       n_outputs,
diff --git a/src/llama-graph.cpp b/src/llama-graph.cpp
index 47a17ef7c..14e5a9dca 100644
--- a/src/llama-graph.cpp
+++ b/src/llama-graph.cpp
@@ -620,6 +620,22 @@ bool llm_graph_input_attn_k_dsa::can_reuse(const llm_graph_params & params) {
     return can_reuse_impl(params);
 }
 
+void llm_graph_input_attn_k_dsa_iswa::set_input(const llama_ubatch * ubatch) {
+    inp_dsa->set_input(ubatch);
+    inp_swa->set_input(ubatch);
+}
+
+bool llm_graph_input_attn_k_dsa_iswa::can_reuse(const llm_graph_params & params) {
+    mctx = static_cast<const llama_kv_cache_dsa_iswa_context *>(params.mctx);
+    inp_dsa->mctx = mctx->get_dsa();
+    inp_swa->mctx = mctx->get_swa();
+
+    bool res = true;
+    res &= inp_dsa->can_reuse_impl(params);
+    res &= inp_swa->can_reuse_impl(params);
+    return res;
+}
+
 bool llm_graph_input_attn_k_dsa::can_reuse_impl(const llm_graph_params & params) {
     bool res = true;
 
@@ -1428,22 +1444,22 @@ void llm_graph_result::set_outputs(const llm_graph_params & params) {
             }
         }
     }
-    for (auto * tensor : t_sampled) {
+    for (const auto & [seq_id, tensor] : t_sampled) {
         if (tensor != nullptr) {
             ggml_set_output(tensor);
         }
     }
-    for (auto * tensor : t_sampled_probs) {
+    for (const auto & [seq_id, tensor] : t_sampled_probs) {
         if (tensor != nullptr) {
             ggml_set_output(tensor);
         }
     }
-    for (auto * tensor : t_sampled_logits) {
+    for (const auto & [seq_id, tensor] : t_sampled_logits) {
         if (tensor != nullptr) {
             ggml_set_output(tensor);
         }
     }
-    for (auto * tensor : t_candidates) {
+    for (const auto & [seq_id, tensor] : t_candidates) {
         if (tensor != nullptr) {
             ggml_set_output(tensor);
         }
@@ -2386,7 +2402,7 @@ ggml_tensor * llm_graph_context::build_moe_ffn(
     // Prefill: the cont/permute of a large expert slab is slower than the
     // classic view/add tree — keep that path for multi-token.
     ggml_tensor * moe_out;
-    if (hparams.n_expert_used == 1) {
+    if (hparams.n_expert_used() == 1) {
         moe_out = ggml_cont(ctx0, ggml_view_2d(ctx0, experts, n_embd, n_tokens, experts->nb[2], 0));
     } else if (n_tokens == 1) {
         ggml_tensor * experts_pe = ggml_cont(ctx0, ggml_permute(ctx0, experts, 1, 0, 2, 3));
@@ -2396,12 +2412,12 @@ ggml_tensor * llm_graph_context::build_moe_ffn(
         // note: use hparams.n_expert_used (not n_expert_used) so warmup stays small
         // ref: https://github.com/ggml-org/llama.cpp/pull/14753
         ggml_tensor * cur_experts[LLAMA_MAX_EXPERTS] = { nullptr };
-        for (uint32_t i = 0; i < hparams.n_expert_used; ++i) {
+        for (uint32_t i = 0; i < hparams.n_expert_used(); ++i) {
             cur_experts[i] = ggml_view_2d(ctx0, experts, n_embd, n_tokens, experts->nb[2], i*experts->nb[1]);
             ggml_build_forward_expand(gf, cur_experts[i]);
         }
         moe_out = cur_experts[0];
-        for (uint32_t i = 1; i < hparams.n_expert_used; ++i) {
+        for (uint32_t i = 1; i < hparams.n_expert_used(); ++i) {
             moe_out = ggml_add(ctx0, moe_out, cur_experts[i]);
             ggml_build_forward_expand(gf, moe_out);
         }
@@ -3989,11 +4005,6 @@ void llm_graph_context::build_sampling() const {
         }
     }
 
-    res->t_sampled.resize(n_rows, nullptr);
-    res->t_sampled_probs.resize(n_rows, nullptr);
-    res->t_sampled_logits.resize(n_rows, nullptr);
-    res->t_candidates.resize(n_rows, nullptr);
-
     // res->t_logits will contain logits for all tokens that want the logits calculated (logits=1 or output=1)
     GGML_ASSERT(res->t_logits != nullptr && "missing t_logits tensor");
 
diff --git a/src/llama-model-loader.cpp b/src/llama-model-loader.cpp
index a9aa4e90c..86fb14c03 100644
--- a/src/llama-model-loader.cpp
+++ b/src/llama-model-loader.cpp
@@ -1096,14 +1096,14 @@ ggml_backend_buffer_type_t llama_model_loader::lazy_read::buft() {
     return ggml_backend_dev_buffer_type(cpu_dev);
 }
 
-bool llama_model_loader::lazy_read::add(const std::string & name, const ggml_tensor * t, const llama_tensor_weight * w) {
-    if (mode == LLAMA_LAZY_MODE_OFF) {
+bool llama_model_loader::lazy_read::add(enum llama_tensor_read_lazy mode, const std::string & name, const ggml_tensor * t, const llama_tensor_weight * w) {
+    if (mode == LLAMA_TENSOR_READ_LAZY_OFF) {
         return false;
     }
 
     // do not lazy-read small tensors, it has significant overhead and is not worth it
     constexpr size_t auto_min_size = 4ull * 1024 * 1024 * 1024;
-    if (mode != LLAMA_LAZY_MODE_ON && ggml_nbytes(t) <= auto_min_size) {
+    if (mode != LLAMA_TENSOR_READ_LAZY_ON && ggml_nbytes(t) <= auto_min_size) {
         return false;
     }
 
@@ -1350,16 +1350,9 @@ struct ggml_tensor * llama_model_loader::create_tensor(
         return NULL;
     }
 
-    if ((flags & TENSOR_READ_LAZY) && use_mmap && tensor_read_lazy != LLAMA_TENSOR_READ_LAZY_OFF) {
-        // in auto mode, small tensors are cheap enough to keep resident
-        constexpr size_t auto_lazy_min_size = 4ull * 1024 * 1024 * 1024;
-        if (tensor_read_lazy == LLAMA_TENSOR_READ_LAZY_ON || ggml_nbytes(cur) > auto_lazy_min_size) {
-            const auto & w = require_weight(tn.str().c_str());
-            lazy_tensor_ranges[w.idx].emplace_back(w.offs, w.offs + ggml_nbytes(cur));
-
-            LLAMA_LOG_INFO("%s: tensor %s (size = %zu MiB) lazy read enabled\n",
-                    __func__, tn.str().c_str(), ggml_nbytes(cur)/1024/1024);
-        }
+    if (flags & TENSOR_READ_LAZY) {
+        // Keep the decision independent from mmap availability for the no-alloc fit pass.
+        is_lazy = lazy.add(tensor_read_lazy, tn.str(), cur, no_alloc ? nullptr : &require_weight(tn.str().c_str()));
     }
 
     ggml_tensor t_meta = *cur;
@@ -1444,11 +1437,8 @@ void llama_model_loader::init_mappings(bool prefetch, llama_mlocks * mlock_mmaps
                 }
             }
 
-            const auto it_lazy = lazy_tensor_ranges.find(idx);
-            static const llama_mmap::ranges no_lazy_ranges;
-
             std::unique_ptr<llama_mmap> mapping = std::make_unique<llama_mmap>(file.get(), prefetch ? -1 : 0, is_numa,
-                    it_lazy != lazy_tensor_ranges.end() ? it_lazy->second : no_lazy_ranges);
+                    lazy.for_file(idx));
             mmaps_used.emplace_back(mapping->size(), 0);
             if (mlock_mmaps) {
                 std::unique_ptr<llama_mlock> mlock_mmap(new llama_mlock());
diff --git a/src/llama-model-loader.h b/src/llama-model-loader.h
index e0cd71cfb..d990d7112 100644
--- a/src/llama-model-loader.h
+++ b/src/llama-model-loader.h
@@ -87,15 +87,27 @@ struct llama_model_loader {
     // set by the caller before the create_tensor() calls
     enum llama_tensor_read_lazy tensor_read_lazy = LLAMA_TENSOR_READ_LAZY_OFF;
 
+    struct lazy_read {
+        bool add(enum llama_tensor_read_lazy mode, const std::string & name, const ggml_tensor * t, const llama_tensor_weight * w);
+        bool any() const { return !ranges.empty(); }
+        bool has(const ggml_tensor * t) const { return tensors.count(ggml_get_name(t)) > 0; }
+        const llama_mmap::ranges & for_file(uint32_t idx) const {
+            static const llama_mmap::ranges none;
+            const auto it = ranges.find(idx);
+            return it == ranges.end() ? none : it->second;
+        }
+        static ggml_backend_buffer_type_t buft();
+    private:
+        std::map<uint32_t, llama_mmap::ranges> ranges;
+        std::set<std::string> tensors;
+    } lazy;
+
     llama_files files;
     llama_ftype ftype;
     llama_fver  fver;
 
     llama_mmaps mappings;
 
-    // byte ranges of TENSOR_READ_LAZY tensors, per file index
-    std::map<uint32_t, llama_mmap::ranges> lazy_tensor_ranges;
-
     std::map<std::string, llama_tensor_weight, weight_name_comparer> weights_map;
     std::unordered_map<std::string, llama_model_kv_override> kv_overrides;
     const llama_model_tensor_buft_override * tensor_buft_overrides;
@@ -219,6 +231,8 @@ struct llama_model_loader {
 
     void unmap_weight(const llama_tensor_weight & w) const;
 
+    const void * load_data_range(const llama_tensor_weight & w, size_t offs, size_t size, void * buf) const;
+
     // for backwards compatibility, does not support ggml-backend
     void load_data_for(struct ggml_tensor * cur) const;
 
diff --git a/src/llama-model-saver.cpp b/src/llama-model-saver.cpp
index 15fbcb856..cbda3c93a 100644
--- a/src/llama-model-saver.cpp
+++ b/src/llama-model-saver.cpp
@@ -482,6 +482,7 @@ void llama_model_saver::add_tensors_from_model() {
     add_tensor(model->hc_head_down);
     add_tensor(model->hc_head_up);
 
+    std::unordered_set<const struct ggml_tensor *> seen;
     for (const struct llama_layer & layer : model->layers) {
         for (size_t i = 0; i < sizeof(layer)/sizeof(struct ggml_tensor *); ++i) {
             const struct ggml_tensor * tensor = reinterpret_cast<const struct ggml_tensor * const *>(&layer)[i];
diff --git a/src/llama-model.cpp b/src/llama-model.cpp
index 42380ac51..c0d37b4b0 100644
--- a/src/llama-model.cpp
+++ b/src/llama-model.cpp
@@ -2569,6 +2569,9 @@ llama_memory_i * llama_model::create_memory(const llama_memory_params & params,
                     (arch == LLM_ARCH_QWEN3NEXT || arch == LLM_ARCH_QWEN35 || arch == LLM_ARCH_QWEN35MOE ||
                      arch == LLM_ARCH_QWEN4EXP);
 
+                const bool mtp_on_hybrid_nemotron =
+                    params.ctx_type == LLAMA_CONTEXT_TYPE_MTP && arch == LLM_ARCH_NEMOTRON_H_MOE;
+
                 if (llm_arch_is_recurrent(arch)) {
                     res = new llama_memory_recurrent(
                             *this,
diff --git a/src/llama-quant.cpp b/src/llama-quant.cpp
index 0aadfba32..17ccee892 100644
--- a/src/llama-quant.cpp
+++ b/src/llama-quant.cpp
@@ -219,6 +219,11 @@ static void llama_tensor_dequantize_impl(
     ggml_tensor * tensor, std::vector<no_init<float>> & output, std::vector<std::thread> & workers,
     const size_t first_elem, const size_t nelements, const int nthread
 ) {
+    if (output.size() < nelements) {
+        output.resize(nelements);
+    }
+    const ggml_type type = tensor->type;
+    float * f32_output = (float *) output.data();
     const ggml_type_traits * qtype = ggml_get_type_traits(type);
     if (ggml_is_quantized(type)) {
         if (qtype->to_float == NULL) {
@@ -1297,6 +1302,8 @@ static void llama_model_quantize_impl(const std::string & fname_inp, const std::
             // no --dry-run, perform quantization
             if (!quantize) {
                 new_size = tensor_size;
+                const void * data = load_range(0, tensor_size);
+                fout.write((const char *) data, new_size);
                 LLAMA_LOG_INFO("size = %8.3f MiB\n", tensor_size/1024.0/1024.0);
             } else {
                 const float * imatrix = nullptr;
@@ -1344,11 +1351,10 @@ static void llama_model_quantize_impl(const std::string & fname_inp, const std::
                 if (work.size() < out_size) {
                     work.resize(out_size);
                 }
-                new_data = work.data();
-
                 const int64_t n_per_row = tensor->ne[0];
                 const int64_t nrows_per_expert = tensor->ne[1];
                 const int64_t nrows_total = tensor->ne[1] * tensor->ne[2];
+                const int64_t nrows = nrows_per_expert;
 
                 const size_t row_size_src = ggml_row_size(tensor->type, n_per_row);
                 const size_t row_size_dst = ggml_row_size(new_type,     n_per_row);
@@ -1394,11 +1400,13 @@ static void llama_model_quantize_impl(const std::string & fname_inp, const std::
                             f32_band = (const float *) f32_conv_buf.data();
                         }
 
-                        void * new_data_band = (char *) new_data + ggml_row_size(new_type, n_per_row) * (i03 * nrows + ir0);
+                        void * new_data_band = work.data() + ggml_row_size(new_type, n_per_row) * (i03 * nrows + ir0);
 
-                        new_size += llama_tensor_quantize_impl(new_type, f32_band, new_data_band, chunk_size, band_rows, n_per_row, imatrix_03, workers, nthread_use);
+                        new_size += llama_tensor_quantize_impl(new_type, f32_band, new_data_band, chunk_size,
+                                i03 * nrows + ir0, band_rows, nrows_per_expert, n_per_row, imatrix, workers, nthread_use);
                     }
                 }
+                fout.write((const char *) work.data(), new_size);
                 LLAMA_LOG_INFO("size = %8.2f MiB -> %8.2f MiB\n", tensor_size/1024.0/1024.0, new_size/1024.0/1024.0);
             }
             total_size_org += tensor_size;
